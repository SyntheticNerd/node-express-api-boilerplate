import User from "./User";

// Set up relationships here
// User.hasMany(Things, { foreignKey: "userId", as: "things" });
// Things.belongsTo(User, { foreignKey: "userId", as: "user" });

export { User };
